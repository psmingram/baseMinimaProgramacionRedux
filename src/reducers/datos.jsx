const initialState = {
    nombreUsuario : 'Javier Mingram',
    idUsuario: 2,
}

export const datos = (state = initialState, action) => {
    return state
}

export const getDatos = state => state